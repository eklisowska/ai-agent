%% ReAct: Retrieve → Act → Reason → Reflect (see agentic_workflow_react.md).
%% Requirements 1–4: README “Requirements Coverage (Agentic Components)” — data prep, RAG, reasoning & reflection, tools.
flowchart TD
    U[User / reviewer]
    API[Go API]

    subgraph idx["1–2 · Prep & RAG index — POST /index"]
        RAW[(Synthetic JSON)]
        PREP[Fact documents]
        Q[(Qdrant)]
        RAW --> PREP --> Q
    end

    subgraph react["ReAct — POST /analyze · /ask"]
        RET[Retrieve]
        ACT[Act — tools]
        REASON[Reason]
        REFLECT[Reflect]
        RET --> ACT --> REASON --> REFLECT
    end

    OUT[RunResult JSON]

    U --> API
    API -->|POST /index| PREP
    Q --> RET
    REFLECT --> OUT

    API -->|POST /analyze /ask| RET
